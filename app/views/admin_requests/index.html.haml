%h1 Admin Dashboard Page

-@courses.each do |course|
= "Course ", course
%table#student_requests
  %thead
    %tr
      %th UIN
      %th Full Name
      %th Major
      %th Classification
      %th Minor
      %th Course ID
      %th Section ID
      %th State
      %th Action
      
  %tbody
    - @student_requests[course].each do |record|
      %tr
        %td= record.uin
        %td= record.full_name
        %td= record.major
        %td= record.classification
        %td= record.minor
        %td= record.course_id
        %td= record.section_id
        %td= record.state
